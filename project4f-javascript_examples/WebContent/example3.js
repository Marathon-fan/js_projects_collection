/**
 * 
 */
function func(){   // string and its attributes and function
	var alpha = "ABCDEFG";
	var length = alpha.length;
	document.write(length);
	document.write(alpha.substring(0, 1));
	document.write(alpha.substring(0, 2));

}
